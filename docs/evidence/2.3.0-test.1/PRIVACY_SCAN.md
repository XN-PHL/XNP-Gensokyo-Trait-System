# Privacy Scan

The public candidate contains only the game runtime tree (`42/` plus required mod metadata, poster, and public documentation). It excludes local `tools/` scripts because historical tooling contains absolute local paths.

Static pattern scan found zero public-candidate matches for local user paths, staging paths, Steam library paths, raw console filenames, saves, credentials, or third-party runtime evidence. Historical engineering documents only state that raw console inclusion is disabled; no raw console is included.
