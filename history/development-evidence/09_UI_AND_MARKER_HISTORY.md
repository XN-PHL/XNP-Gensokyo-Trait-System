# UI And Marker History

Recovered evidence covers the four-marker migration, circular rendering, runtime draw repair, drag capture, map hiding, tooltip wiring, red icon correction, and green screen-coordinate failures. The 0.5.60.6.4 green orb does not use an `ISPanel` or screen-position authority; existing four marker UI files are preserved from the baseline.

## 0.5.60.6.9 Red Guardian restoration

- The Red marker lock source is the canonical Red CharacterTrait result only.
- Inventory count zero remains visible in the tooltip/counter but does not grey or lock a valid owner.
- The extra rectangular disabled overlay was removed; the approved Red center texture remains unchanged.
- Right-click persists `GREEN_STAMINA` or `WHITE_TREATMENT`; double-left-click queues the existing craft TimedAction.
- Drag capture, map hiding, shared shell rendering, and the other three marker panels are unchanged.

## 0.5.60.6.10 Red legacy exact restore

- The Red marker now receives its trait gate from the proven local OBJECT_COLLECTION detector.
- Right-click mode switching and double-left TimedAction crafting remain the 0.5.60.6.9 implementations and become reachable when the restored gate resolves true.
- Marker lock state remains trait-only; inventory count zero does not add a grey overlay or disabled border.
- Yellow, Purple and Green UI modules and all four approved marker image assets are unchanged.
