# XNP Gensokyo Trait System

Current channel: `V2_TEST`  
Current version: `2.3.0-test.1`  
Build marker: `XNP_V2_230_TEST1_MULTI_RECORD_INHERITANCE_A`

This is the separate Project Zomboid Build 42.20 test channel. It must not be enabled with the stable channel. The test release adds owner-scoped multi-record Extra Life inheritance: each successful record creates an immutable record ID, while restore uses the selected ID, replaces character traits exactly once, and grants only a quarter positive non-Fitness/Strength XP gap.

Runtime and multiplayer verification remain pending. Workshop upload is intentionally user-controlled.

See `SOURCE/` for the installable runtime tree and `docs/evidence/2.3.0-test.1/` for sanitized static evidence.
