RUNTIME TEST ITEM - NOT UPLOADED BY CODEX

The Contents tree is a public-only runtime mirror.
Private runtime evidence, raw logs, local paths, and development audit archives are excluded.
GreenRuntimeTestNoCooldown defaults to true in this test build and must default to false before a stable release.
GreenInflightDiagnosticBorderEnabled defaults to true in this test build and must default to false before a stable release.
